// Renium 3-way merge tests live in the repo outputs folder (renium-merge-test/__merge_test.mts).
// Removed from src so `tsc -p .` is not affected. Run with: node --experimental-strip-types <file>
export {};
