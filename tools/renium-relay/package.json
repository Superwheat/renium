{
  "name": "renium-relay",
  "version": "0.3.10",
  "private": true,
  "description": "Cloudflare Worker that hosts Renium collaboration rooms",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "check": "tsc -p . --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "lib0": "^0.2.100",
    "y-protocols": "^1.0.6",
    "yjs": "^13.6.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260916.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
